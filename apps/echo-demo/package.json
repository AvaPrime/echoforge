{
  "name": "echo-demo",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start -p ${PORT:-3000}",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "clean": "rimraf .next build dist"
  },
  "dependencies": {
    "@echoforge/echocore": "workspace:*",
    "@echoforge/echoui": "workspace:*",
    "@echoforge/blueprint": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.0",
    "vitest": "^3.2.4"
  }
}
