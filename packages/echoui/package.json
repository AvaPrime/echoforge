{
  "name": "@echoforge/echoui",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --dts --sourcemap --format cjs,esm --out-dir dist",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@echoforge/codessa": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsup": "^8.0.2",
    "vitest": "^3.2.4",
    "rimraf": "^5.0.0"
  }
}
