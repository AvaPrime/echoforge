apiVersion: apps/v1
kind: Deployment
metadata:
  name: echo-cloud
  labels:
    app: echo-cloud
spec:
  replicas: 2
  selector:
    matchLabels:
      app: echo-cloud
  template:
    metadata:
      labels:
        app: echo-cloud
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
      containers:
        - name: echo-cloud
          image: ghcr.io/echoforge/echo-cloud:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: NODE_ENV
              value: 'production'
            - name: PORT
              value: '3000'
          resources:
            requests:
              cpu: '100m'
              memory: '128Mi'
            limits:
              cpu: '500m'
              memory: '512Mi'
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ['ALL']
---
apiVersion: v1
kind: Service
metadata:
  name: echo-cloud
  labels:
    app: echo-cloud
spec:
  type: ClusterIP
  selector:
    app: echo-cloud
  ports:
    - name: http
      port: 80
      targetPort: 3000
